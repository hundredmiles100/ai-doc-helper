import ShaderBrainBackground from './ShaderBrainBackground'

// keep same import name for compatibility — now shader + brain video
export default function AnimatedBackground() {
  return <ShaderBrainBackground />
}
